<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kaktusx.menuex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".exercise1"
            android:label="Calculator" />
        <activity
            android:name=".exercise2"
            android:label="Constraint Layout_A" />
        <activity
            android:name=".exercise2_b"
            android:label="Constraint Layout_B" />
        <activity
            android:name=".exercise2_c"
            android:label="Constraint Layout_C" />
    </application>

</manifest>